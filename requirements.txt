# LiveChat API Platform — backend dependencies

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Google Gemini Live API SDK
google-genai>=1.0.0

# Database
motor>=3.4.0              # async MongoDB driver (Motor wraps PyMongo)
pymongo>=4.7.0            # Motor peer dependency + index constants
redis[hiredis]>=5.0.0     # async Redis client; hiredis = fast C parser

# Security
argon2-cffi>=23.1.0       # API key hashing (and later password hashing)

# HTTP client
httpx>=0.27.0             # async webhook calls in tool_executor

# Data validation
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Utilities
python-dotenv>=1.0.0